import variable from './../variables/material';

export default (variables = variable) => {
    const tabTheme = {
        flex: 1,
        backgroundColor: 'transparent'
    };

    return tabTheme;
};
